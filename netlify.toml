# Netlify deployment configuration
# Publish directory set to "dist" to match Vite's default build output
[build]
  command = "npm run build"
  publish = "dist"

# SPA routing - redirect all requests to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
